{
  "name": "laen-server",
  "version": "1.0.0",
  "description": "LAEN Express server + static frontend for deployment (Render/Heroku)",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.0",
    "cors": "^2.8.5",
    "express-rate-limit": "^6.7.0"
  }
}
